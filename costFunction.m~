function J = costFunction(theta,X,y)

    m = length(y);
    J = 0;
    cost = 0;
    
    linear = X*theta(1:2);
    nonLinear = theta(sigmoid(linear,theta(3),theta(4),theta(5));
    cost =(nonLinear-y);
    cost = cost.^(2);
    cost = sum(cost,1);
    J = cost/(2*m);
    
end